<div class="col-lg-12">
    <div class="card rounded-0 shadow mt-5 mb-5">
      <div class="card-body">
        <p style="color: green"><%= notice %></p>

        <%= render @course %>

        <div class="form-group">
          <%= link_to 'Back', admin_courses_path, class: "btn btn-danger btn-lg" %> | <%= link_to 'Edit', edit_admin_course_path(@course), class: "btn btn-warning btn-lg" %>
        </div>
      </div>
    </div>
</div>