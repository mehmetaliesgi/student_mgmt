<div class="col-lg-12">
  <div class="card rounded-0 shadow mt-5 mb-5">
    <div class="card-body">
      <p style="color: green"><%= notice %></p>

      <%= render @project %>

      <div class="form-group">
        <%= link_to "Back to projects", admin_projects_path, class: "btn btn-danger btn-lg" %> | <%= link_to "Edit this project", edit_admin_project_path(@project), class: "btn btn-primary btn-lg" %>
      </div>
    </div>
  </div>
</div>